--- AEscadaEAPerguntaScene/baseline-font-only
+++ AEscadaEAPerguntaScene/master
@@ -884,14 +884,22 @@
         self.play(LaggedStart(*[ShowPassingFlash(plate[0].copy().set_fill(opacity=0).set_stroke(self.green,width=4)) for plate in model],lag_ratio=0.15),run_time=0.9)
         token=self.card("próximo token",self.green,size=34).set_width(3.6).move_to([4.4,0.2,0])
         self.at(35.94,GrowFromPoint(token,model.get_right()),duration=0.8)
-        self.at(37.1,token.animate.scale(0.04).move_to(model.get_top()),model.animate.set_width(5.5).move_to([0,-0.5,0]),duration=1.0)
-        self.remove(token)
-        # A short repeated processing callback, then the already-known capabilities.
-        for t in [38.5,40.0]:
-            self.at(t,ShowPassingFlash(model[-1][0].copy().set_fill(opacity=0).set_stroke(self.green,width=4)),duration=0.7)
-        self.at(41.4,model.animate.set_width(6.5).move_to(ORIGIN),duration=1.4)
+        context=self.label("O cachorro correu atrás da",40,self.ink,[0,1.2,0])
+        if context.width>11:context.scale_to_fit_width(11)
+        answer=self.label("bola",60,self.green,[0,-0.1,0])
+        self.at(37.1,FadeOut(token),model.animate.set_width(3.4).move_to([0,-1.8,0]),FadeIn(context),duration=1.0)
+        self.at(38.5,ShowPassingFlash(model[-1][0].copy().set_fill(opacity=0).set_stroke(self.green,width=4)),duration=0.6)
+        self.at(39.2,GrowFromPoint(answer,model.get_top()),duration=0.6)
+        self.at(40.0,answer.animate.next_to(context,DOWN,buff=0.2),duration=0.7)
+        self.at(41.4,FadeOut(context),FadeOut(answer),model.animate.set_width(3.4).move_to([0,-1.8,0]),duration=1.4)
+        # Familiar examples return at the corresponding spoken capabilities.
         task=None
-        for t,word in [(43.62,"Traduzir"),(44.8,"Programar"),(45.86,"Resumir"),(46.98,"Responder"),(48.16,"Resolver")]:
-            nxt=self.label(word,56,self.green,[2.5,0,0])
-            self.at(t,*([FadeIn(nxt,shift=RIGHT),model.animate.set_width(5.5).move_to([-3.5,0,0])] if task is None else [FadeTransform(task,nxt)]),duration=0.8);task=nxt
+        examples=[(43.62,"cachorro → dog"),(44.8,"return a + b"),(45.86,"Chuva → alagamento"),(46.98,"POSITIVO"),(48.16,"2 + 2 → 4")]
+        for t,words in examples:
+            nxt=self.label(words,48,self.green,[0,0.8,0])
+            if nxt.width>10:nxt.scale_to_fit_width(10)
+            animations=[ShowPassingFlash(model[-1][0].copy().set_fill(opacity=0).set_stroke(self.green,width=4))]
+            if task is not None:animations.append(FadeOut(task,shift=UP*0.15))
+            self.at(t,Succession(AnimationGroup(*animations),FadeIn(nxt,shift=UP*0.15)),duration=0.8)
+            task=nxt
         self.finish(49.166666667)
--- AEscalaDosGPTsScene/baseline-font-only
+++ AEscalaDosGPTsScene/master
@@ -922,11 +922,13 @@
         echoes=VGroup()
         positions=[[-4.8,1.7,0],[4.8,1.7,0],[-4.8,0,0],[4.8,0,0],[-4.8,-1.9,0],[4.8,-1.9,0]]
         for index,position in zip([0,1,2,3,5,6],positions):
-            echo=self.completed_outputs[index].copy()
+            short={0:"dog",1:"POSITIVO",2:"Buscou a bola.",3:"return a + b",5:"Alagamento",6:"4"}
+            echo=self.text(short[index],36,self.green)
             echo.scale(min(3.6/echo.width,0.72/echo.height)).move_to(position).set_opacity(0.95)
             echoes.add(echo)
         task_title.set_width(6.4).move_to([0,3.3,0])
-        self.at(98.0,FadeOut(self.task_output),Transform(machine,self.front_view),self.camera.frame.animate.set_width(15.5).move_to([0,0.4,0]),LaggedStart(*[FadeIn(e,shift=e.get_center()/15) for e in echoes],lag_ratio=0.08),Write(task_title),duration=0.8)
+        self.at(98.0,FadeOut(self.task_output),Transform(machine,self.front_view),self.camera.frame.animate.set_width(15.5).move_to([0,0.4,0]),duration=0.55)
+        self.at(98.55,LaggedStart(*[FadeIn(e) for e in echoes],lag_ratio=0.08),Write(task_title),duration=0.25)
         self.checkpoint("six-completed-results-payoff")
         self.at(101.4,LaggedStart(*[e.animate.scale(0.025).move_to(machine.get_center()).set_opacity(0) for e in echoes],lag_ratio=0.08),duration=0.6)
         self.remove(echoes,*echoes)
@@ -976,6 +978,7 @@
         target=profile if close_processing else self.front_view.copy()
         if output_focus:target.move_to([-2.6,-0.35,0])
         side=layout==0 and len(self.task_records)>0
+        if side:target.shift(RIGHT*1.25)
         views=[([4.6,0.2,0],11.8,[3.8,2.1,0],RIGHT) if side else ([0,0,0],14.22,[-4.9,0,0],LEFT),
                ([0,0.4,0],10.6,[0,2.5,0],UP),
                ([0,0.55,0],16.5,[-4.9,0,0],LEFT)]
@@ -991,7 +994,9 @@
         if instruction:
             strip=self.card(instruction,self.ink,size=34,width=5.8).move_to([-4.6,1.65,0])
             strip[0].set_stroke(self.purple,width=3).set_fill("#29334D",opacity=1)
-            heading=self.label("PERSONA",22,self.paper,[-4.6,2.45,0])
+            strip.set_width(min(strip.width,6.5))
+            strip[1].set_color(WHITE).set_opacity(1).set_z_index(10)
+            heading=self.label("PERSONA",22,WHITE,[-4.6,2.45,0]).set_z_index(10)
             route=Line(strip.get_bottom(),incoming.get_top()+UP*0.12,color=self.purple,stroke_width=3)
             panel=VGroup(strip,heading,route)
             self.play(FadeIn(strip,shift=DOWN*0.15),FadeIn(heading),Create(route),run_time=0.45)
--- ANovaFinalizacaoScene/baseline-font-only
+++ ANovaFinalizacaoScene/master
@@ -844,13 +844,13 @@
 class ANovaFinalizacaoScene(Director, VisualScene, MovingCameraScene):
     def construct(self):
         self.begin()
-        model=self.machine().set_width(5.5).move_to([-3.5,0,0]);task=self.label("Resolver",56,self.green,[2.5,0,0]);self.add(model,task)
+        model=self.machine().set_width(3.4).move_to([0,-1.8,0]);task=self.label("2 + 2 → 4",48,self.green,[0,0.8,0]);self.add(model,task)
         how=self.label("COMO",45,self.paper,[-4.5,2.5,0])
         this=self.label("ISSO",62,self.ink,[-2.1,1.0,0])
         happens=self.label("ACONTECE",78,self.cyan,[0,-0.4,0]).set_width(9.8)
         mark=self.label("?",90,self.yellow,[5.7,-0.4,0])
         question=VGroup(how,this,mark)
-        self.at(0.6,FadeOut(model),ReplacementTransform(task,how),duration=0.5)
+        self.at(0.6,FadeOut(model),Succession(FadeOut(task),Write(how)),duration=0.5)
         self.at(1.2,Write(this),duration=0.45)
         self.at(1.7,Write(happens),duration=0.8)
         self.at(2.65,Write(mark),duration=0.35)
--- OLimiteDoDicionarioScene/baseline-font-only
+++ OLimiteDoDicionarioScene/master
@@ -863,9 +863,19 @@
     def construct(self):
         self.begin()
         self.camera.frame.set_width(12.8).move_to([0.3,0.15,0])
-        book=self.open_book().set_width(7.5);self.add(book)
+        # Exact continuation of the opening book, child and travelling first word.
+        book=self.open_book().set_width(7).move_to([-1.6,0,0])
         child=self.person().scale(1.2).move_to([5,-0.5,0])
-        self.at(2.1,book.animate.set_width(7).move_to([-1.6,0,0]),FadeIn(child,shift=LEFT),duration=2)
+        word=self.label("casa",30,self.cyan,[1.8,1.5,0])
+        self.follow_word(child,word);child[2].update(0)
+        self.add(book,child,word)
+        line=list(reversed(list(book[3])[:5]))[0]
+        scan=line.copy().set_stroke(self.cyan,width=4,opacity=1).set_z_index(7)
+        self.play(Transform(word,self.on_rule(self.text("casa",30,self.cyan),line)),Create(scan),run_time=1.1)
+        child[2].clear_updaters()
+        self.play(word.animate.set_color(self.green),scan.animate.set_color(self.green),run_time=0.25)
+        self.play(VGroup(*child[:4]).animate(rate_func=there_and_back).shift(DOWN*0.12),run_time=0.3)
+        self.at(2.1,FadeOut(word),FadeOut(scan),Transform(child,self.person().scale(1.2).move_to([5,-0.5,0])),duration=0.6)
         self.at(5.24,self.confused(child),duration=0.8)
         self.play(self.blink(child),run_time=0.35)
         # Derive every baseline from the actual transformed page geometry.
--- OQueCriouOChatGPTScene/baseline-font-only
+++ OQueCriouOChatGPTScene/master
@@ -916,9 +916,29 @@
         system=self.label("um sistema",58,self.green,[0,3,0])
         self.at(37.53,Write(system),duration=1.5)
         self.at(39.55,self.camera.frame.animate.set_width(14.22).move_to([0,-0.25,0]),duration=2.5)
-        book=self.open_book().set_width(7.5)
-        self.at(44,FadeOut(result,scale=0.8),FadeOut(system),GrowFromCenter(book),self.camera.frame.animate.set_width(12.8).move_to([0.3,0.15,0]),duration=2)
+        invite=self.label("VAMOS LÁ",72,self.ink,[0,0,0])
+        self.at(44,Succession(AnimationGroup(FadeOut(result,scale=0.8),FadeOut(system)),Write(invite)),duration=1.0)
+        # Match the vertical stem of L to the spine; the other letters retire locally.
+        stem=invite[5]
+        axis=Line([stem.get_left()[0]+0.02,stem.get_bottom()[1],0],[stem.get_left()[0]+0.02,stem.get_top()[1],0],color=self.ink,stroke_width=3)
+        book=self.open_book().set_width(7).move_to([-1.6,0,0])
+        self.add(axis)
+        self.at(45.05,FadeOut(invite,shift=UP*0.12),Transform(axis,book[2]),duration=0.7)
+        opened=book.copy()
+        hinge=book[2].get_x()
+        book.apply_function(lambda p:np.array([hinge+(p[0]-hinge)*0.035,p[1],p[2]]))
+        self.remove(axis);self.add(book)
+        self.at(45.8,Transform(book,opened),self.camera.frame.animate.set_width(12.8).move_to([0.3,0.15,0]),duration=1.6)
+        child=self.person().scale(1.2).move_to([5,-0.5,0])
+        word=self.label("casa",30,self.cyan,[3,2.3,0])
+        def follow(eyes):
+            delta=word.get_center()-child[0].get_center()
+            eyes.move_to(child[0].get_center()+UP*0.065+delta/(np.linalg.norm(delta)+1e-8)*0.095)
+        child[2].add_updater(follow)
+        self.at(47.4,FadeIn(child,shift=LEFT*0.25),FadeIn(word,shift=DOWN*0.3),duration=0.6)
+        self.at(48.0,word.animate.move_to([1.8,1.5,0]),duration=0.7)
         self.finish(48.75)
+        child[2].clear_updaters()
 
     def assembled_tools(self):
         # A control enclosure with ports and routed tools: never another network.
--- PosTreinamentoEChatGPTScene/baseline-font-only
+++ PosTreinamentoEChatGPTScene/master
@@ -926,9 +926,13 @@
         self.at(53.1,Create(tuned),duration=1.4)
         # Evolution: the tuned base is absorbed into the assistant composition.
         self.at(54.89,VGroup(base,tuned).animate.move_to([3.6,0,0]).set_width(3.2),FadeOut(base_label),logo.animate.set_opacity(0.25),duration=1.4)
-        product=self.browser("Assistente",width=5.3,height=4.1).move_to([3.6,0,0]);product.set_z_index(-1)
-        self.at(58.4,ReplacementTransform(tuned,product),base.animate.scale(0.08).move_to(logo),logo.animate.set_opacity(1),FadeOut(chat),duration=1.6)
+        product=self.browser("Assistente",width=5.3,height=4.1).move_to([0,0,0]);product.set_z_index(-1)
+        product[3].set_x(product[0].get_x())
+        self.at(58.4,ReplacementTransform(tuned,product[0]),base.animate.scale(0.08).move_to(logo),FadeOut(logo),FadeOut(chat),duration=1.2)
         self.remove(base)
-        prompt=self.label("Como posso ajudar?",28,self.ink,[3.6,-1.2,0])
+        logo.set_x(0).set_opacity(1)
+        self.play(FadeIn(VGroup(*product[1:])),FadeIn(logo),run_time=0.4)
+        self.remove(product[0],*product[1:]);self.add(product)
+        prompt=self.label("Como posso ajudar?",28,self.ink,[0,-1.2,0])
         self.play(Write(prompt),run_time=1.6)
         self.finish(65.75)
