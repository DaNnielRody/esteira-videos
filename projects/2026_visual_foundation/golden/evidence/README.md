# Visual foundation evidence

This accepted golden project is deterministic evidence for the production theme,
bounded geometry, a transform, semantic colours, text/MathTex layout, measurable
timing, and a recurring vector across two ordered scenes. The SVGs are intentionally
small keyframes; no binary MP4 comparison or provider call is part of regression.
